const styles = (theme) => ({
  root: {
    display: 'flex',
    paddingLeft: theme.spacing(1),
    alignItems: 'center',
    flex: '1 1 auto',
  },
  input: {
    flex: '1 1 auto',
  },
});

export default styles;
