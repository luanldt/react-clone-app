const styles = (theme) => ({
  root: {
    display: 'flex',
    flexDirection: 'column',
    height: '100vh',
  },
  contentWrapper: {
    position: 'relative',
    flex: '1 1 auto',
  },
});

export default styles;
